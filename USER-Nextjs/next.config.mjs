/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['booking-stadium.online'], // <--- ใส่โดเมนที่ต้องการ
    },
};

export default nextConfig;
